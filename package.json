{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "test": "pulp test"
  },
  "dependencies": {
    "text-encoding": "^0.6.4",
    "tweetnacl": "^1.0.0-rc.1"
  },
  "devDependencies": {
    "pulp": "^11.0.0",
    "purescript": "^0.11.4",
    "purescript-psa": "^0.5.1",
    "rimraf": "^2.6.1"
  }
}
