{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "test": "pulp test"
  },
  "dependencies": {
    "tweetnacl": "^0.14.3"
  },
  "devDependencies": {
    "pulp": "^9.0.1",
    "purescript": "^0.10.1",
    "purescript-psa": "^0.3.9",
    "rimraf": "^2.5.4",
    "text-encoding": "^0.6.1"
  }
}
